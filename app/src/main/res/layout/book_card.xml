<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_view"
    android:layout_width="200dp"
    android:layout_height="240dp"
    app:cardUseCompatPadding="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Front cover image of the book -->
        <ImageView
            android:id="@+id/front_cover_art_image_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/book_cover_image"
            android:scaleType="fitXY"
            tools:src="@drawable/book_cover_sample"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:background="@drawable/scrim"
            android:orientation="vertical">

            <!-- Title of the book -->
            <TextView
                android:id="@+id/book_title_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingEnd="@dimen/min_dimension"
                android:paddingLeft="@dimen/min_dimension"
                android:paddingRight="@dimen/min_dimension"
                android:paddingStart="@dimen/min_dimension"
                android:textColor="@android:color/white"
                tools:text="Title"/>

            <!-- Author of the book -->
            <TextView
                android:id="@+id/author_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingEnd="@dimen/min_dimension"
                android:paddingLeft="@dimen/min_dimension"
                android:paddingRight="@dimen/min_dimension"
                android:paddingStart="@dimen/min_dimension"
                android:textColor="@android:color/white"
                tools:text="Author"/>

        </LinearLayout>


    </RelativeLayout>

</android.support.v7.widget.CardView>